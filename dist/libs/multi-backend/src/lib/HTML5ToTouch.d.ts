import { BackendTransition } from 'dnd-multi-backend';
import { BackendFactory } from 'dnd-core';
export declare const HTML5ToTouch: {
    backends: BackendTransition[];
};
export declare function createDefaultMultiBackend(): BackendFactory;
